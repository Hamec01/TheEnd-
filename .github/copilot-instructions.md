# Workspace Notes

- Follow monorepo boundaries: UI logic in frontend, game rules in shared domain, persistence and orchestration in backend.
- Do not hardcode race/stat formulas inside React components.
- Keep combat calculations reusable via shared domain helpers.
- Before continuing the universal item effects roadmap, read `docs/COPILOT_TZ_HANDOFF.md` for current implementation status and constraints.
